{
  "pipeline": "custom-docling-parser-integration",
  "requirementId": "DOCLING-01",
  "status": "waiting",
  "note": "Previous state.json (pipeline: loopover-batch-pipeline, requirementId: OPT-05-09) was discarded during flow-engine bootstrap on 2026-07-15: it claimed design-convert and human-confirm-design were 'completed' for OPT-05-07 and OPT-08-09 with artifacts at flow-engine/workspace/*/design.md, but that workspace directory was empty and no design doc or human confirmation ever actually occurred. It did not reflect real pipeline runs.",
  "trueCompletionStatus": "See flow-engine/.bootstrap.md for a code-verified cross-check of todo.md against actual app/ code as of bootstrap time. Summary: todo.md items 1-5 and 7 are genuinely done; item 6 (sentence-window/hierarchical chunk) is also already done despite todo.md marking it pending; item 8 (prompt citations+CoT) is partially done (citations/CoT present, no dedicated refusal-quality pass); item 9 (conversation memory) is not done (session_id field and {history} prompt placeholder exist but are unwired). Items 10-15 not verified.",
  "nodes": {
    "design": {
      "status": "completed",
      "artifacts": { "designDocPath": "./design.md" },
      "note": "Design doc covers: (1) PDF_PARSER=docling for PDF/Office via DoclingReader+DoclingNodeParser, (2) CHUNK_MODE=layout_aware new option dispatching .md->MarkdownNodeParser, .json->JSONNodeParser, other->SentenceSplitter fallback, shared via new _parse_documents_to_nodes() helper used by all three PDF_PARSER branches. Embedding infra explicitly unchanged (local HuggingFaceEmbedding). User confirmed with '是' after revision."
    },
    "code-impl": {
      "status": "completed",
      "artifacts": {
        "changedFiles": [
          "app/config.py",
          "app/rag.py",
          "app/main.py",
          "requirements.txt",
          ".env.example",
          "tests/test_rag_docling_parser.py"
        ]
      },
      "note": "Implemented per design.md: pdf_parser Literal += 'docling', chunk_mode Literal += 'layout_aware'. RagService gained _build_docling_nodes() (DoclingReader+DoclingNodeParser for PDF/Office, with manual file_name/file_path metadata restore since DoclingReader doesn't auto-populate it and DoclingNodeParser overwrites node.metadata with its own chunk metadata) and _parse_documents_to_nodes() (shared by default/pymupdf4llm/docling-fallback branches; layout_aware dispatches .md/.json/other to MarkdownNodeParser/JSONNodeParser/SentenceSplitter). main.py's /api/index/rebuild now injects Settings and appends a docling timing hint. requirements.txt pinned to llama-index-readers-docling==0.2.1-compatible range and llama-index-node-parser-docling==0.2.0-compatible range (installed and verified against llama-index-core 0.11.x, no core upgrade needed). Implementation quality gate self-checked: imports/compiles OK, naming conventions followed, matches design doc.",
      "installedPackages": ["llama-index-readers-docling==0.2.1", "llama-index-node-parser-docling==0.2.0", "docling==2.113.0 (transitive)"]
    },
    "test": {
      "status": "waiting",
      "note": "tests/test_rag_docling_parser.py: 16 tests (was 12; added 4 for rebuild_index() dispatch across docling/default/pymupdf4llm/empty-result branches, since rebuild_index had 0% coverage before this change). Full suite: 47 passed, 0 failed, 0 regressions. Coverage gate item 'Coverage >= 80%' NOT met: overall 63% (up from 55% pre-change; app/rag.py 56%->67%, all DOCLING-01 code (lines 174-297) now fully covered). Shortfall is entirely pre-existing debt outside design.md scope: app/main.py 0% (no FastAPI TestClient tests exist at all) and RagService.__init__/_vector_store/_storage_context/_ensure_index/_build_reranker/_build_llamaindex_llm (rag.py:46-152) 0%, both requiring live-DB/embedding mocking infra that doesn't exist yet. 'All tests pass' and 'Critical paths tested' (the new dispatch logic) are satisfied. Gate is failureStrategy=block per flow-engine/nodes/unit-test-run.md -> escalated to human-confirm rather than silently marked completed."
    },
    "code-review": {
      "status": "completed",
      "artifacts": { "reviewReport": "./review.md" },
      "note": "Self-review per flow-engine/nodes/code-review.md (failureStrategy=skip). No critical issues found. Two proactively-caught metadata bugs (DoclingReader shared extra_info dict, DoclingNodeParser metadata overwrite) documented as fixed. Recommendations: main.py/RagService constructor test coverage gap flagged as future work, not blocking."
    }
  }
}
