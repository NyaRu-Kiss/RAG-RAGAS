# RAG Eval 实施计划

## 1. 目标

建立只覆盖检索链路和生成链路的离线评测流程：

- 检索层暂不计算 MRR、HitRate、Recall、nDCG 等需要 gold ID/qrels 的指标。
- 检索层重点记录完整 pipeline 轨迹，支持从生成结果倒查检索、rerank 和上下文拼接问题。
- 生成层使用 Ragas 的 AI-as-a-Judge 评测回答质量。
- 每次运行保存配置、输入、检索轨迹、最终上下文、生成结果和评测分数，支持后续基线比较。

评测样本分为两个固定集合：

- 200 条 retrieval 集：暂不运行检索指标；保留为未来引入 gold IDs/qrels 后计算 Retrieval metrics 的固定集。
- 20 条 generation 集：每次生成层评测均执行完整 RAG pipeline，并运行 Ragas Judge。

## 2. 明确范围

### 纳入评测

1. 检索链路
   - query transform / multi-query / HyDE 等实际启用的查询变换。
   - 初始 retriever 返回的节点及排序分数。
   - `fetch_k`、初始候选数量和检索策略。
   - reranker 是否启用、rerank 前候选数、rerank 后节点数及最终排序。
   - 最终传给生成 LLM 的 chunk 顺序、文本、来源、页码和分数。
2. 生成链路
   - user input。
   - 最终上下文。
   - LLM response。
   - reference answer 和 reference contexts（数据集提供时）。
   - Ragas `faithfulness`、`response_relevancy`、`factual_correctness`（确认当前 Ragas 版本 API 后启用）。
3. 运行审计
   - app/eval/judge 配置。
   - pipeline 各阶段的耗时、异常和数量统计。
   - 数据集路径、数据集哈希、运行时间和代码版本信息。
4. 数据集分层执行
   - generation 集固定为 20 条，只用于完整 RAG pipeline、生成回答和 Ragas 评测。
   - retrieval 集固定为 200 条，只为未来 Retrieval metrics 预留；当前只准备和固化该集合，不执行 RAG、Ragas、MRR、HitRate 或其他检索指标。

### 暂不纳入评测

- 不准备 gold document/chunk IDs。
- 不计算 MRR、HitRate、Recall、MAP、nDCG 等需要 qrels 或 gold IDs 的检索质量指标。
- 不把 chunking/parser 作为独立评分层。
- 不做在线评测、多轮对话评测和自动 prompt 优化。

说明：chunking 不单独打分，但必须记录 chunk 相关配置和最终 chunk 内容，便于从检索结果反查 chunking 影响。

## 3. 已确认的决策

| 项目 | 决策 |
| --- | --- |
| 数据集规模 | 200 条固定 retrieval 集；20 条固定 generation 集。 |
| 当前检索指标 | 暂不准备 gold IDs/qrels，故不计算 MRR、HitRate、Recall、MAP、nDCG。 |
| Judge 配置 | 通过 `.env.eval` 配置第三方 OpenAI-compatible API 的 key、base URL、model 与可选 temperature。 |
| Ragas embedding | `response_relevancy` 复用 RAG App 当前配置的本地 embedding 模型；不额外调用第三方 embedding API。 |
| Eval 索引隔离 | 每个数据集使用固定、独立的 PostgreSQL eval 表；重建只清空对应 eval 表，不触及开发/生产表。 |
| Baseline | 手动指定一个已确认的 run 作为 baseline；后续运行只与它比较，不自动覆盖。 |
| 初始门禁 | 第一阶段只输出 delta 和报告，不阻断；累计 3-5 次稳定运行后再设置阈值。 |
| 可迭代范围 | 允许调整 top_k、fetch_k、query transform、reranker、prompt、embedding、chunk/parser 配置；不修改公开 benchmark 的原始样本。 |

## 4. 现有实现与目标差距

当前 `eval/` 已有：

- JSONL 数据集模型和 tag/limit 过滤。
- RAG adapter，可获得回答、contexts 和 citations。
- Ragas `context_precision`、`response_relevancy`、`faithfulness` 的基础调用。
- manifest、samples、summary、failures 文件输出。

需要调整或补充：

- 将检索轨迹从 `RagService` 暴露给 eval adapter，而不是只保留最终文本。
- 明确并记录初始检索、rerank、最终上下文三个阶段。
- 记录实际发送给生成 LLM 的完整 prompt/context、模板版本、token 数和所有上下文处理行为。
- 生成层只保留适合回答质量的 Ragas 指标，并让 `response_relevancy` 复用 App 本地 embedding；检索阶段暂不使用需要 gold IDs 的指标。
- 缺失数据集或过滤后无样本时禁止静默生成空评测报告。
- 增加运行指纹、逐样本阶段记录、严格的 baseline 可比性校验和生成层 baseline/delta 能力。
- 定义 pipeline/Judge 失败、跳过和 NaN 的聚合与 baseline 对齐规则。

## 5. 目标数据流

```text
EvalSample
  -> query
  -> query transform
  -> base retriever
  -> reranker
  -> final contexts
  -> generation LLM
  -> Ragas judge
```

每个样本都要保存上述阶段的输入、输出和统计信息。最终上下文必须与实际发送给生成 LLM 的内容一致，不能只保存 UI citation snippet。

除最终 chunk 列表外，还必须保存：

- 实际发送给生成 LLM 的完整序列化 context。
- system prompt / user prompt 模板标识与模板内容哈希。
- chunk 的原始排序、去重、截断、拼接结果及每一步的 token 数。
- 最终请求的 input token 数、output token 数（provider 可返回时）和生成模型参数。

必要时 prompt 正文可保存为仅限本地的 artifact；即使因敏感信息不能持久化正文，也必须保存其确定性哈希、长度、模板版本和完整 context，以便确认两次运行是否使用相同输入。

## 6. 检索轨迹记录设计

每个样本的 `retrieval_trace` 至少包含：

```json
{
  "query": "原始问题",
  "transformed_queries": [],
  "retrieval_mode": "vector|hybrid|fusion|hyde",
  "top_k": 5,
  "fetch_k": 20,
  "candidate_count": 20,
  "reranker_enabled": true,
  "rerank_input_count": 20,
  "rerank_output_count": 5,
  "retrieved_nodes": [
    {
      "rank": 1,
      "node_id": "可用时记录，暂不用于 qrels",
      "score": 0.0,
      "file_name": "...",
      "file_path": "...",
      "page_label": "...",
      "text": "完整 chunk 文本"
    }
  ],
  "final_contexts": [
    {
      "rank": 1,
      "node_id": "...",
      "text": "实际送入生成 LLM 的 chunk 文本"
    }
  ],
  "generation_input": {
    "system_prompt_template_id": "...",
    "system_prompt_hash": "...",
    "user_prompt_template_id": "...",
    "user_prompt_hash": "...",
    "serialized_context": "实际送入 LLM 的完整上下文",
    "context_token_count": 0,
    "request_token_count": 0,
    "context_operations": ["deduplicate", "truncate"]
  },
  "timings_ms": {
    "query_transform": 0.0,
    "retrieve": 0.0,
    "rerank": 0.0,
    "generation": 0.0
  }
}
```

要求：

- 记录初始候选和 rerank 后结果，不能只记录最后五个 chunk。
- 记录实际生效的配置值，不能只记录环境变量默认值。
- `node_id` 即使暂时不参与指标，也应保留，方便日志去重和后续升级到 qrels。
- 记录上下文截断、去重、排序或拼接行为；若没有这些行为，也明确记录为空。
- 记录完整的 generation input，使 trace 可验证“最终 contexts”确实等于真实 prompt 中的上下文。

## 7. 生成层 Ragas 设计

### 输入

使用实际运行得到的：

- `user_input`
- `response`
- `retrieved_contexts` / `final_contexts`
- `reference`
- `reference_contexts`（可选）

`reference` 和 `reference_contexts` 只允许作为 Ragas Judge 的评测输入；生成 LLM 的 system prompt、user prompt、retrieved contexts、final contexts 和任何 query transform 输入中不得包含它们。运行时必须对实际 generation input 做泄漏断言：若检测到 reference 字段或完整 reference context 被注入生成输入，该样本标记为 `invalid`，不调用 Judge、不参与聚合或 baseline 比较。

### 指标

- `faithfulness`：回答是否被最终上下文支持。
- `response_relevancy`：回答是否针对问题。
- `factual_correctness`：回答与 reference 的事实一致性，需先确认当前 Ragas 版本是否可用。

`context_precision/context_recall` 暂不作为本阶段检索门禁，避免在没有 gold IDs/qrels 的情况下产生不清晰的指标解释。

### Judge 约束

- 固定 judge provider、model、base URL 和评测 prompt 版本。
- 尽量使用 temperature=0 或 provider 支持的最低随机性配置。
- API key、base URL、model、temperature、timeout、retry 和并发数均从 `.env.eval` 读取；不得将第三方 API 凭据写入代码或 run artifact。
- `response_relevancy` 的 embeddings 直接复用 RAG App 当前启用的本地 embedding 模型与其配置；manifest 记录模型标识、版本和配置哈希，不记录无关凭据。
- 记录 timeout、retry、并发数和失败原因。
- 保存逐样本分数，不能只保存均值。
- 对 judge 返回的 NaN、超时和解析失败单独计数。

## 8. 运行产物

每次运行目录保存：

```text
manifest.json       # 数据集/代码/app/eval/judge/pipeline 配置
samples.jsonl       # 每个样本的回答、最终上下文、citations、Ragas 分数
retrieval_traces.jsonl  # 每个样本的完整检索与 rerank 轨迹
summary.json        # 聚合均值、分位数、失败数、分桶统计
summary.md          # 人类可读报告
failures.json       # pipeline 或 judge 失败
comparison.json     # 指定 baseline 时的可比性检查、逐样本对齐和指标 delta
```

运行产物采用原子完成语义：先写入同级临时目录，完成所有 artifact 写入、校验和生成和 summary 汇总后，再原子改名为最终 run 目录，并在 `manifest.json` 写入 `status: completed`。任何异常、中断、磁盘写入失败或 preflight 失败都必须保留或清理为明确的 `failed` / `partial` 状态，且这类 run 不得作为 baseline、不得参与 report 或 delta。

`manifest.json` 至少记录：

- UTC timestamp 和唯一 run ID。
- Git commit SHA（无法取得时明确标记）。
- 数据集绝对路径、内容哈希、选中样本数和过滤条件。
- retriever、query transform、reranker、top_k、fetch_k、最终 context 数量。
- chunk/parser/embedding 配置快照，仅用于追踪，不作为本阶段独立指标。
- 生成 LLM 和 Ragas judge 配置。
- App 本地 embedding 模型标识、版本和配置哈希。
- generation prompt/context 模板版本及哈希。

配置快照只能保留 provider、base URL（按安全需要可脱敏）、model 和非敏感运行参数；API key 不得写入 manifest、summary、trace 或日志。

### 失败、跳过与聚合规则

- 每项 Ragas 指标分别记录 `attempted`、`scored`、`failed`、`skipped` 和 `nan` 数量。
- RAG pipeline 失败、Judge 超时、解析失败和 NaN 不得转换为 0 分或混入指标均值。
- 聚合均值、分位数和 baseline delta 只基于该指标有效的 `scored` 样本。
- `samples.jsonl` 必须保留每项指标的状态、原始错误类型和错误信息；`failures.json` 聚合相同失败。
- baseline 比较时按 source sample ID 对齐；只在 current 与 baseline 均有有效同名指标分数的样本上计算 delta，同时单独报告新增失败、恢复成功、缺失样本和不可比较样本。

## 9. 报告与后续 EDD 门禁

第一阶段先实现可观测性和 Ragas 分数，不立即设定最终阈值。报告需要支持：

- 全量均值和样本数。
- 按 tag、difficulty、question_type 的分桶统计。
- 失败样本列表和完整 retrieval trace 链接/字段。
- 当前运行与 baseline 的 Ragas 指标 delta。

baseline 比较的前置检查：

- current 与 baseline 的 generation 集 source sample IDs、样本数和 dataset hash 必须完全一致，否则拒绝输出 aggregate delta。
- 两次运行的 Ragas 指标集合必须一致；缺少或待验证指标只标记不可比较，不能补零。
- RAG pipeline、Judge、embedding 或 prompt 配置允许不同，因为它们是实验变量；报告必须在 `comparison.json` 和 `summary.md` 中列出结构化配置 diff。

待积累稳定基线后，再定义：

- faithfulness 最低阈值。
- response relevancy/factual correctness 最低阈值。
- 失败率上限。
- 相对 baseline 的最大允许回归幅度。

## 10. 测试数据策略

不要求手工编写初始评测集，优先使用公开、可下载、带标准答案的开源数据集。每次导入都要记录来源版本、下载日期、抽样 seed、原始文件哈希和转换脚本版本，确保后续能复现同一批样本。

### 第一阶段：HotpotQA distractor validation

将 HotpotQA distractor validation 作为第一个离线回归集：

- 有问题、标准答案、supporting facts 和候选文档，适合多跳问答。
- 用于观察检索候选、rerank、最终上下文与 Ragas 生成指标。
- 固定抽取 200 条 validation 样本作为未来 retrieval 集；从其中确定性抽取 20 条作为当前日常 generation 回归集。
- generation 集是 retrieval 集的子集，便于未来关联同一问题的检索与生成结果。
- 用共享语料库构建索引：语料库固定为 200 条 retrieval 样本的所有候选 Wikipedia 段落去重后的全集；每个原始段落只进入 corpus 一次，所有 query 共用同一索引。
- corpus 文档使用稳定的 `title + paragraph_index` 作为原始文档身份；转换时保留 source sample IDs 与段落来源映射，去重规则和最终 corpus hash 写入 `dataset_manifest.json`。
- 不得为每个问题生成只包含该问题标准证据的专属文档；这种构造会泄漏答案证据并让检索评测失真。
- 继续保留 HotpotQA 的 supporting facts/段落信息，即使当前不准备 gold IDs；它们可用于人工核验检索 trace，并为未来引入 qrels 留出空间。

### 第二阶段：通用补充集

- RAGBench：补充端到端 RAG 生成质量与失败归因场景。
- BEIR：提供成熟的 corpus、query 和 qrels。当前可只使用 corpus/query/参考答案和 trace；将来决定评估 MRR、HitRate、Recall 或 nDCG 时，可直接复用其 qrels，不需要重建数据集。

### 第三阶段：领域数据集

根据真实业务文档类型选择最接近的公开集，例如金融、医疗、法律、技术文档或企业知识库。通用数据集只能验证 pipeline 能力，不能证明真实业务质量。

长期仍建议加入少量经匿名化处理的真实用户问题，作为公开数据集之外的回归集；这一步不属于当前第一阶段的前置条件。

## 11. 数据集导入与运行规则

1. 原始下载文件放在不与代码混杂的位置，并保存数据集 license、URL、版本和校验和。
2. 转换后的 eval dataset 必须保存数据源 sample ID，禁止只使用重新编号后的本地 ID。
3. corpus 与 query dataset 分开保存；同一数据集的所有 query 必须查询同一个 corpus 索引。
4. HotpotQA corpus 固定为 200 条 retrieval 集的候选段落去重全集；不得仅用 generation 20 条或某一 query 的候选段落建库。
5. 固定抽样 seed、样本数量和 tag 过滤条件，并把这些信息写入 manifest。
6. 固定段落 identity、去重规则和 corpus hash；任一变化均视为不同 dataset，不能直接与旧 baseline 比较。
7. 先对少量样本人工检查：最终 contexts 是否来自共享 corpus、是否与生成 prompt 一致、trace 是否保留了来源。
8. 未确认数据集 license 可用于当前用途前，不将数据或派生语料提交到仓库。

## 12. 端到端工作流与命令契约

### 数据获取和准备

1. 提供 `eval data fetch` 命令，从固定 URL 下载指定版本的公开数据集。
2. 提供 `eval data prepare` 命令，将原始数据转换为共享 corpus、query 数据集、20 条 generation 集和预留的 200 条 retrieval 集。
3. 准备命令写出 `dataset_manifest.json`：来源 URL、license、版本、下载日期、原始文件哈希、转换版本、抽样 seed、各集合的 source sample IDs。
4. 准备命令为该数据集创建或选择独立的 PostgreSQL eval 表；只允许操作预先批准的 `eval_*` 表名。
5. 提供 `eval index rebuild --dataset <name>`，只重建对应共享 corpus 的独立 eval 表，并输出索引配置、文档数、chunk 数和耗时。
6. 索引重建完成后，将目标 eval 表、corpus hash、文档数、chunk 数和索引配置哈希写入索引状态文件；该状态是 generation eval 的前置条件。

### 评测运行

1. `eval run generation --dataset <name>`：默认只执行 20 条 generation 集；每条运行完整 RAG、记录 retrieval trace、生成回答，并调用 Ragas。
2. `eval run retrieval --dataset <name>`：当前不提供执行入口；200 条 retrieval 集只作为已固化的未来评测集。未来 qrels 准备完成后，实现该命令并优先启用 LlamaIndex `RetrieverEvaluator` 的 MRR/HitRate 等指标；仅当 LlamaIndex 不覆盖目标指标时才使用 `pytrec_eval`。
3. 通过 `--baseline <run_id_or_path>` 手动指定 baseline；没有该参数时只产生当前 run，不自动更新任何 baseline。
4. 通过 `.env.eval` 读取第三方 API 配置。建议配置键：

```dotenv
EVAL_JUDGE_PROVIDER=openai_compatible
EVAL_JUDGE_API_KEY=...
EVAL_JUDGE_BASE_URL=https://third-party.example/v1
EVAL_JUDGE_MODEL=...
EVAL_JUDGE_TEMPERATURE=0
EVAL_TIMEOUT_SECONDS=120
EVAL_MAX_RETRIES=2
EVAL_MAX_WORKERS=1
```

5. `eval report --run <run_id_or_path>`：从保存的 artifacts 重新生成可读报告，不重新调用 RAG 或 Judge。
6. `eval run generation` 在任何实际 RAG/Judge 调用前运行 preflight：验证 20 条 generation 集、共享 corpus manifest、独立 eval 表、eval 表的索引状态与 corpus hash 是否一致、App 本地 embedding、Judge API 配置和 Ragas 指标兼容性；失败时退出非零且不创建成功样式的 run 目录。

### 人工核验与 RAG 迭代

1. 初始版本先运行 20 条 generation 集，人工抽查低分或失败样本。
2. 按以下顺序定位：final contexts 不足或不相关 -> 检查初始候选与 rerank trace -> 检查 query transform/top_k/fetch_k -> 检查 chunk/parser/embedding -> 检查 prompt 和生成模型。
3. 每次只改变一个假设或一组紧密相关的参数，并记录变更说明。
4. 重新运行相同的 20 条 generation 集，并手动对比指定 baseline 的逐样本和聚合 delta。
5. 连续 3-5 次运行稳定后，才为 Ragas 指标、失败率和性能数据设定 EDD 门禁；门禁配置独立版本化。
6. 当未来准备 gold IDs/qrels 后，对固定 200 条 retrieval 集启用检索指标，使用相同 corpus、抽样 seed 和结果存储结构。

## 13. 实施顺序

1. 确认当前 LlamaIndex retriever、query transform 和 reranker 的实际调用边界。
2. 设计检索轨迹数据结构，并让 eval adapter 能获得完整阶段数据。
3. 保存实际 final contexts，确保与生成 LLM 输入一致。
4. 扩展 manifest 和逐样本 artifacts，加入 pipeline 配置与耗时。
5. 整理 Ragas 生成指标、第三方 Judge 配置和 App 本地 embedding 复用方式，增加非空 runner 集成测试。
6. 实现 preflight、失败/NaN 统计与严格的 baseline 可比性校验。
7. 增加分桶聚合、失败样本报告、配置 diff 和 baseline delta。
8. 用固定 20 条 generation 集做人工核验，确认 corpus 来源、final contexts 与真实 prompt 一致。
9. 在多次稳定运行后，再确定 EDD 阈值和是否引入 gold IDs/qrels。

## 14. 验收标准

- 一次 eval 能确认实际使用的 `top_k`、`fetch_k`、reranker 状态和 rerank 前后数量。
- 每个样本能看到最终发送给生成 LLM 的 chunk 及其来源。
- 每个样本能追溯实际 generation prompt 的 context、模板版本、上下文处理行为和 token 统计。
- 每个样本能看到回答、reference、Ragas 各项分数和 judge/pipeline 失败信息。
- 缺失数据集或零样本不会被当成成功评测。
- 报告能按数据集过滤条件和 pipeline 配置复现一次运行。
- 当前阶段不依赖 gold IDs，也不会输出伪造的 MRR/HitRate 等检索质量结论。
- HotpotQA 等公开集通过共享 corpus 运行，未按 query 注入对应 gold evidence。
- generation 命令只对固定 20 条集合调用完整 RAG 与 Ragas；固定 200 条 retrieval 集当前不执行，未来仅用于检索指标评测。
- 第三方 Judge API 完全从 `.env.eval` 配置，API key 不会出现在任何评测产物中。
- eval 索引重建只能影响对应独立 `eval_*` PostgreSQL 表，不能影响开发或生产表。
- `reference` 与 `reference_contexts` 不会进入 generation LLM 的实际输入；检测到泄漏的样本会标记 invalid 并排除。
- run 仅在全部 artifact 原子落盘后标记 `completed`；`failed` 或 `partial` run 不能被比较、报告或作为 baseline。
- generation eval preflight 必须确认独立 eval 表的索引状态与目标 corpus hash 一致；旧语料索引不得参与新数据集运行。
- 每次参数迭代可通过同一 20 条 generation 集和手动 baseline 做逐样本回溯。
- baseline 仅在数据集、20 条 source sample IDs 和指标集合一致时输出 aggregate delta；失败、NaN 或不一致样本不会被当作 0 分。
- HotpotQA eval corpus 始终是固定 200 条 retrieval 集候选段落去重全集，并具有可复现的 identity、去重规则和 corpus hash。
